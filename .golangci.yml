run:
  concurrency: 4
linters-settings:
  misspell:
    locale: US
  gocritic:
    disabled-checks:
      - commentFormatting
linters:
  enable-all: true
  disable:
    - lll # long lines
    - depguard
    - gochecknoglobals
    - interfacebloat
    - ireturn
    - varnamelen
    - err113 # remove later!
    - wrapcheck # remove later!
    - noctx # remove later!
    - execinquery # deprecated
    - gomnd # deprecated
