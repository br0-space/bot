version: "2"
run:
  concurrency: 4
linters:
  default: all
  enable:
    - wsl_v5
  disable:
    - depguard
    - err113
    - gochecknoglobals
    - interfacebloat
    - ireturn
    - lll
    - noctx
    - varnamelen
    - wrapcheck
    - noinlineerr
    - wsl # replaced by wsl_v5
  settings:
    gocritic:
      disabled-checks:
        - commentFormatting
    misspell:
      locale: US
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
    rules:
      - path: interfaces\/
        linters:
          - revive
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
